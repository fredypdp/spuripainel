// src/app/(painel)/avaliacoes/page.tsx
import React from "react";
import { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Avaliações",
};

export default function AvaliacoesPage() {
  return <PageContent />;
}