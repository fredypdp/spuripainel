"use client";
import React, { useEffect, useState } from "react";
import { adminService } from "@/lib/api/services";
import type { StorageQuotaResponse } from "@/types/api";
export default function PageContent(){ const [q,setQ]=useState<StorageQuotaResponse|null>(null); const [erro,setErro]=useState(""); useEffect(()=>{adminService.getStorageQuota().then(setQ).catch((e:any)=>setErro(e?.message??"Erro ao consultar quota"));},[]); const pct=q&&q.total_bytes?Math.min(100,Math.round(q.used_bytes/q.total_bytes*100)):0; return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-gray-900 dark:text-white">Armazenamento</h1><p className="text-sm text-gray-500">Quota do provider externo usado pelos documentos de matrícula.</p></div>{erro&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{erro}</p>}{q?<div className="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-gray-900"><div className="mb-4 flex justify-between"><span className="text-sm uppercase text-gray-500">Provider: {q.provider}</span><span className="font-semibold text-gray-900 dark:text-white">{pct}% usado</span></div><div className="h-3 overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800"><div className="h-full bg-brand-500" style={{width:`${pct}%`}} /></div><div className="mt-6 grid gap-4 md:grid-cols-3">{[["Total",q.total_human],["Usado",q.used_human],["Disponível",q.available_human]].map(([l,v])=><div key={l} className="rounded-xl bg-gray-50 p-4 dark:bg-gray-800"><p className="text-sm text-gray-500">{l}</p><p className="mt-1 text-xl font-bold text-gray-900 dark:text-white">{v}</p></div>)}</div></div>:<div className="h-44 animate-pulse rounded-2xl bg-gray-100 dark:bg-gray-800"/>}</div> }
