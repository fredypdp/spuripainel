import PageContent from "./PageContent";
import { Metadata } from "next";
export const metadata: Metadata = { title: "Solicitações de matrícula" };
export default function Page(){ return <PageContent />; }
