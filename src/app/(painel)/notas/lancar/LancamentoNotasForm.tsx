"use client";import{useCallback,useEffect,useState}from'react';import{jobApiService,tokenStorage}from'@/lib/api';import{useUserCookie}from'@/hooks/useUserCookie';import{dividirEmLotes}from'../../estudantes/cadastrar/massaHelpers';import SelecaoContextoNotas from'./SelecaoContextoNotas';import UploadPlanilhaNotas from'./UploadPlanilhaNotas';import RelatorioValidacaoNotas,{LIMITE_NOTAS_POR_LOTE}from'./RelatorioValidacaoNotas';import BatchProgressScreenNotas from'./BatchProgressScreenNotas';import type{ContextoModeloNotas,ResultadoAnaliseNotas}from'./notasTypes';import{construirPayloadNota}from'./notasPayload';import{registrarNotasBatch}from'./notasApi';import{lerRascunhoNotas,salvarRascunhoNotas}from'./notasDraft';
type Fase={tipo:'verificando'}|{tipo:'normal'}|{tipo:'progresso';jobIds:string[];contexto:ContextoModeloNotas|null;avisoSubmissao?:string|null};export default function LancamentoNotasForm(){const{user}=useUserCookie();const[fase,setFase]=useState<Fase>({tipo:'verificando'}),[ctx,setCtx]=useState<ContextoModeloNotas|null>(null),[res,setRes]=useState<ResultadoAnaliseNotas|null>(null),[nome,setNome]=useState(''),[env,setEnv]=useState(false),[erro,setErro]=useState('');useEffect(()=>{let a=true;(async()=>{try{const token=tokenStorage.get();if(!token){setFase({tipo:'normal'});return}const {jobs}=await jobApiService.list(token);const ativos=(jobs||[]).filter(j=>j.type==='registrar_nota_batch'&&(j.status==='pending'||j.status==='processing'));const r=lerRascunhoNotas();if(a)setFase(ativos.length?{tipo:'progresso',jobIds:ativos.map(j=>j.id),contexto:r?.contexto??null}:{tipo:'normal'});}catch{if(a)setFase({tipo:'normal'})}})();return()=>{a=false}},[]);const reiniciar=useCallback(()=>{setCtx(null);setRes(null);setNome('');setErro('');setFase({tipo:'normal'})},[]);async function confirmar(){if(!res?.contexto)return;setEnv(true);setErro('');const payload=res.linhas.map(l=>construirPayloadNota(l,res.contexto!));const lotes=dividirEmLotes(payload,LIMITE_NOTAS_POR_LOTE);const jobIds:string[]=[];let enviados=0,aviso:null|string=null;salvarRascunhoNotas({contexto:res.contexto,nomeArquivo:nome,jobIds,itensPendentes:payload});for(const lote of lotes){try{const r=await registrarNotasBatch(lote,tokenStorage.get()||undefined);jobIds.push(r.job_id);enviados+=lote.length;salvarRascunhoNotas({contexto:res.contexto,nomeArquivo:nome,jobIds,itensPendentes:payload});}catch(e:any){aviso=`Foram enviadas ${enviados} de ${payload.length} nota(s). Não foi possível enviar as demais: ${e?.message||'erro desconhecido'}.`;break}}setEnv(false);if(jobIds.length)setFase({tipo:'progresso',jobIds,contexto:res.contexto,avisoSubmissao:aviso});else setErro(aviso||'Não foi possível iniciar o lançamento.')}if(fase.tipo==='verificando')return <div className="flex items-center justify-center py-16"><span className="h-8 w-8 animate-spin rounded-full border-2 border-gray-300 border-t-brand-500"/></div>;if(fase.tipo==='progresso')return <BatchProgressScreenNotas jobIds={fase.jobIds} contexto={fase.contexto} avisoSubmissao={fase.avisoSubmissao} onConcluido={reiniciar}/>;return <div className="space-y-5"><SelecaoContextoNotas onModeloGerado={setCtx}/>{ctx&&!res&&<div className="rounded-lg border border-blue-200 bg-blue-50 p-3 text-sm text-blue-700">Modelo baixado para <strong>Turma {ctx.turmaLabel??ctx.codigoTurma} — {ctx.periodoLabel} — {ctx.materiaNome} — {ctx.categoriaNome}</strong>. Preencha e envie o ficheiro abaixo.</div>}<UploadPlanilhaNotas onResultado={(r,n)=>{setRes(r);setNome(n);setErro('')}}/>{res&&<RelatorioValidacaoNotas resultado={res} nomeArquivo={nome} enviando={env} erroEnvio={erro} onConfirmar={confirmar} onNovoUpload={()=>{setRes(null);setNome('');setErro('')}}/>}</div>}
