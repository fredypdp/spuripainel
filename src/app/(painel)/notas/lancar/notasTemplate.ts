import * as XLSX from 'xlsx';
import type { ContextoModeloNotas } from './notasTypes';
export const COLUNAS_NOTAS = ['Nome do Estudante','Código do Estudante','Valor da Nota'];
function slugify(value:string){return value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');}
export function gerarNomeArquivoModeloNotas(contexto: ContextoModeloNotas){return `modelo-lancamento-notas-${slugify(contexto.codigoTurma)}-${slugify(contexto.periodoLabel || contexto.periodo)}.xlsx`;}
function aplicarCabecalho(ws:any){COLUNAS_NOTAS.forEach((h,c)=>{const a=XLSX.utils.encode_cell({r:0,c});ws[a]={...(ws[a]||{t:'s',v:h}),s:{fill:{patternType:'solid',fgColor:{rgb:'ABDBE3'}},font:{bold:true,color:{rgb:'000000'}},alignment:{horizontal:'center',vertical:'center',wrapText:true}}};});ws['!rows']=[{hpt:30}];ws['!autofilter']={ref:`A1:${XLSX.utils.encode_col(COLUNAS_NOTAS.length-1)}1`};}
export function gerarModeloExcelNotas(contexto: ContextoModeloNotas, estudantes:{nome:string;codigo_estudante:string}[]):void{const wb=XLSX.utils.book_new(); const inst=[['Modelo de Lançamento de Notas — Spuri'],[],['Academia',contexto.nomeAcademia],['Turma',contexto.turmaLabel||contexto.codigoTurma],['Curso',contexto.cursoNome||''],['Ano Acadêmico',contexto.anoAcademicoLabel],['Período',contexto.periodoLabel],['Matéria',contexto.materiaNome],['Categoria',contexto.categoriaNome],['Gerado em',new Date().toLocaleString('pt-PT')],[],['Instruções'],['1. Preencha a nota na folha "Notas", coluna "Valor da Nota", a partir da linha 2.'],['2. Não altere cabeçalhos, nomes de colunas, nem o nome/ordem das folhas.'],['3. Não é preciso preencher turma, período, matéria ou categoria — este modelo já está definido para esse contexto.'],['4. Linhas com "Valor da Nota" em branco são ignoradas no envio.'],['5. A nota deve ser um número entre 0 e 20 (aceita casas decimais, ex.: 14.5).'],['6. Não adicione nem remova linhas de estudantes.']]; const wi=XLSX.utils.aoa_to_sheet(inst); wi['!cols']=[{wch:34},{wch:64}]; XLSX.utils.book_append_sheet(wb,wi,'Instruções'); const rows=[COLUNAS_NOTAS,...[...estudantes].sort((a,b)=>a.nome.localeCompare(b.nome,'pt',{sensitivity:'base'})).map(e=>[e.nome,e.codigo_estudante,''])]; const ws:any=XLSX.utils.aoa_to_sheet(rows); ws['!cols']=[{wch:30},{wch:22},{wch:16}]; aplicarCabecalho(ws); for(let r=1;r<rows.length;r++) for(let c=0;c<COLUNAS_NOTAS.length;c++){const a=XLSX.utils.encode_cell({r,c}); ws[a]={...(ws[a]||{t:'s',v:''}),z:'@'};} XLSX.utils.book_append_sheet(wb,ws,'Notas'); const meta=[['chave','valor'],['versao_modelo',contexto.versaoModelo],['codigo_academia',contexto.codigoAcademia],['nome_academia',contexto.nomeAcademia||''],['nivel',contexto.nivel],['curso_id',contexto.cursoId||''],['curso_nome',contexto.cursoNome||''],['ano_academico',contexto.anoAcademico],['ano_academico_label',contexto.anoAcademicoLabel],['codigo_turma',contexto.codigoTurma],['turma_label',contexto.turmaLabel||''],['periodo',contexto.periodo],['periodo_label',contexto.periodoLabel],['materia_disciplinar_id',contexto.materiaId],['materia_nome',contexto.materiaNome],['categoria',contexto.categoria],['categoria_nome',contexto.categoriaNome],['tipo_nota',contexto.tipoNota],['gerado_em',new Date().toISOString()]]; XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet(meta),'_meta'); const idx=wb.SheetNames.indexOf('_meta'); (wb as any).Workbook={Sheets:wb.SheetNames.map((_,i)=>i===idx?{Hidden:1}:{})}; XLSX.writeFile(wb,gerarNomeArquivoModeloNotas(contexto));}
